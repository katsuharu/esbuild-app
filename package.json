{
  "name": "esbuild-app",
  "version": "0.0.1",
  "description": "first esbuild app",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild src/index.jsx --servedir=public --outdir=public/src --bundle",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sasaki-katsuharu",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.10",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
